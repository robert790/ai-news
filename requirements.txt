# Ziarul Digital · runtime dependencies
# Kept minimal for fast Hugging Face Spaces deploy

# Web framework
# Need >= 1.40 for st.pills (used by the Prompts tab pill filters).
streamlit>=1.40.0

# HTTP + HTML scraping
httpx>=0.27.0
beautifulsoup4>=4.12.0

# LLM (DeepSeek is OpenAI-compatible)
openai>=1.30.0

# Date handling
python-dateutil>=2.8.0